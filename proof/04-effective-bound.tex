\documentclass[12pt]{article}
\usepackage{amsmath,amsthm,amssymb,mathtools}
\usepackage{hyperref}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{remark}[theorem]{Remark}

\newcommand{\A}{\mathbb{A}}
\DeclareMathOperator{\Res}{Res}
\DeclareMathOperator{\Ad}{Ad}

\title{Explicit Lower Bound: $L(1, \mathrm{sym}^2 f) \geq c_{\mathrm{eff}}/\log N$\\
\large Proof Document 04 --- sym2-effective-lbound\\
\normalsize \textbf{Status: [OBL] --- Proof obligations not yet closed}}
\author{}
\date{}

\begin{document}
\maketitle

\noindent\textbf{Warning.}  This document contains the main theorem statement and
proof strategy.  The result is [OBL] pending the closure of proof/03-mollifier.tex
obligations.  Do not use this as a proved theorem.

\begin{abstract}
This document states the main research goal and its proof strategy:
for any normalized primitive Hecke eigenform $f$ of prime level $p$ with trivial
nebentypus, prove
\[
  L(1, \mathrm{sym}^2 f) \geq \frac{c_{\mathrm{eff}}}{\log p}
\]
with an explicit, computable constant $c_{\mathrm{eff}} > 0$.
The proof follows Goldfeld--Hoffstein--Lieman (1994), but with all implicit constants
made explicit via rigorous interval arithmetic.  The key novelty is a computer-certified
value of $c_{\mathrm{eff}}$.
\end{abstract}

\tableofcontents

\section{Main Theorem Statement [OBL]}

\begin{theorem}[Main, [OBL]]\label{thm:main}
Let $f \in S_k(p)$ be a normalized primitive Hecke eigenform of weight $k \geq 2$
and prime level $p$.  Then
\[
  L(1, \mathrm{sym}^2 f) \geq \frac{c_{\mathrm{eff}}}{\log p},
\]
where $c_{\mathrm{eff}}$ is an explicit positive constant, independent of $f$ and $p$,
certified by interval arithmetic in \texttt{src/numerical\_delta.py}.
\end{theorem}

\begin{remark}[Current status]
The theorem is [OBL].  The following are [THM] and available:
\begin{itemize}
  \item Theorem~F-1 (local Euler factor factorization): [THM]
\end{itemize}
The following remain [OBL]:
\begin{itemize}
  \item Theorem~F-2 (global residue positivity): the exact adjoint input and
        positive bad-local correction factors are not yet supplied
  \item Theorem~F-3 ($L(1, \mathrm{sym}^2 \Delta) \in [L_{\mathrm{lo}}, L_{\mathrm{hi}}]$ certified):
        the prior value $2.405$ was retracted (wrong RS identity); correct discovery
        estimate $\approx 0.631793$, pending certified control of the AFE dual term.
  \item Mollifier mean value estimate (proof/03-mollifier.tex, Prop.~M-1)
  \item Zero-free region with explicit constant (proof/03-mollifier.tex, Prop.~M-2)
  \item Explicit contour integral constant (this document, Section~3)
\end{itemize}
\end{remark}

\section{Proof Strategy: Goldfeld--Hoffstein--Lieman Dichotomy}

\begin{definition}[Siegel zero]
A Siegel zero for $L(s, \mathrm{sym}^2 f)$ is a real zero $\beta$ satisfying
\[
  \beta > 1 - \frac{c_0}{\log(p(2+|t|))}
\]
for a constant $c_0 > 0$ to be made explicit.
\end{definition}

\subsection{Case 1: No Siegel Zero}

\begin{proposition}[Case 1: explicit bound from zero-free region, [OBL]]\label{prop:case1}
Assume $L(s, \mathrm{sym}^2 f)$ has no Siegel zero.  Then for an explicit $c_1 > 0$:
\[
  L(1, \mathrm{sym}^2 f) \geq \frac{c_1}{\log p}.
\]
\end{proposition}

\begin{proof}[Proof strategy]
From the zero-free region of width $\delta = c_0/\log p$, the Hadamard product
representation of $L(s, \mathrm{sym}^2 f)$ near $s = 1$ gives
\[
  -\frac{L'}{L}(s, \mathrm{sym}^2 f) = \sum_\rho \frac{1}{s-\rho} + O(\log p)
\]
with the sum over zeros controlled by the zero-free region.  Integrating along the
segment $[1-\delta, 1]$ and exponentiating yields
\[
  \log L(1, \mathrm{sym}^2 f) \geq -C \log\log p
\]
for an explicit $C$, giving $L(1, \cdot) \geq (\log p)^{-C}$.  The stronger bound
$c_1/\log p$ (with explicit $c_1$) follows from the full Goldfeld--Hoffstein--Lieman
calculation, which requires tracing the constant in the log-free zero-free region.
\textbf{[OBL: close this argument with explicit $c_1$.]}
\end{proof}

\subsection{Case 2: Exceptional Zero, Quantitative Obligation}

\begin{proposition}[Case 2: explicit exceptional-zero bound, {[OBL]}]\label{prop:case2}
If $\beta$ is a Siegel zero for $L(s, \mathrm{sym}^2 f)$ with $\beta > 1 - c_0/\log p$,
then $L(1, \mathrm{sym}^2 f) \geq c_2/\log p$ for an explicit $c_2 > 0$.
\end{proposition}

\begin{proof}[Proof strategy]
For $\beta=1-\varepsilon$ with $\varepsilon=O(1/\log p)$ and some
$\xi\in(\beta,1)$,
\[
  L(1,\mathrm{sym}^2 f)=L(\beta,\cdot)+L'(\xi,\cdot)\varepsilon
  =L'(\xi,\cdot)\varepsilon .
\]
Thus the desired bound requires a uniform explicit lower bound for
$L'(\xi,\cdot)$ on this interval.  Even the desired pointwise F-2 input would
supply neither uniform positivity nor a derivative bound.  Deriving and
certifying such a quantitative input is open.
\textbf{[OBL: prove the required uniform logarithmic-derivative or Taylor
coefficient bound, then make $c_2$ explicit.]}
\end{proof}

\subsection{Conclusion}

Combining Cases 1 and 2 with $c_{\mathrm{eff}} = \min(c_1, c_2)$ yields
Theorem~\ref{thm:main}.  The constant $c_{\mathrm{eff}}$ will be certified by
interval arithmetic once the two case constants are computed.

\section{Explicit Constant Computation Plan [OBL]}

To compute $c_{\mathrm{eff}}$ explicitly, we need:

\begin{enumerate}
  \item \textbf{Analytic conductor bound.}  For $f \in S_k(p)$, the analytic conductor of
        $\Pi = \mathrm{sym}^2 f$ satisfies $q(\Pi) \leq Cp^2$ for an explicit $C$
        (depending only on the weight $k$).  This follows from the conductor formula
        for symmetric square lifts via the Gelbart--Jacquet theory.
  \item \textbf{Log-free zero-free region.}  An explicit version of the Vinogradov--Korobov
        zero-free region for $GL_3$ $L$-functions gives: $L(s, \Pi) \neq 0$ for
        $\mathrm{Re}(s) > 1 - c_3 / (\log q(\Pi) \cdot \log\log q(\Pi))^{2/3}$
        with explicit $c_3$ from \cite{Vin58,Kor58}.
  \item \textbf{Hadamard product constants.}  The functional equation for $L(s, \Pi)$
        (Shimura \cite{Sh75}, completed by Gelbart--Jacquet \cite{GJ78}) gives the
        completed $L$-function $\Lambda(s, \Pi) = q(\Pi)^{s/2} L_\infty(s, \Pi) L(s, \Pi)$
        satisfying $\Lambda(s) = \varepsilon(\Pi) \Lambda(1-s, \tilde\Pi)$.
        From this, the density of zeros is controlled by $\log q(\Pi)$.
  \item \textbf{Exceptional-zero constant.}  From the Taylor expansion at a
        potential Siegel zero, obtain a uniform explicit lower bound for the
        relevant logarithmic derivative or first Taylor coefficient.  The
        the desired pointwise residue positivity F-2 would be insufficient for this
        step; the required quantitative Rankin--Selberg input must be proved.
  \item \textbf{Interval arithmetic certification.}  Once $c_1$ and $c_2$ are expressed
        as explicit rational functions of the above quantities, \texttt{python-flint}/Arb
        will certify the numerical values with outward-rounded interval arithmetic.
\end{enumerate}

\section{Numerical Grounding: $N=1$ (Ramanujan $\Delta$) [OBL]}

For $f = \Delta \in S_{12}(SL_2(\mathbb{Z}))$ (level $N=1$), Theorem~F-3 is [OBL].

\textbf{Prior claim retracted.}  An earlier computation reported
$L(1, \mathrm{sym}^2 \Delta) \approx 2.406$ and certified a lower bound of $2.405$.
This result was erroneous: the Rankin--Selberg identity used was
$\sum_n \tau(n)^2/n^{11+s} = \zeta(s) \cdot L(s, \mathrm{sym}^2\Delta)$
(missing the $\zeta(2s)^{-1}$ denominator), and the $\tau(47)$ table entry was wrong.
The retraction is recorded in \texttt{src/numerical\_delta.py} and \texttt{tests/test\_numerical.py}.

\textbf{Correct value (discovery tier).}  Three independent methods converge to
\[
  L(1, \mathrm{sym}^2 \Delta) \approx 0.631,
\]
specifically: RS Tauberian (N=5000): $0.6314$; Dirichlet series ($s=1.01$, N=3000): $0.634$;
GL$_3$ AFE partial sum: $0.631$.  None of these is a certified bound.

\textbf{Certification path.}  A certified interval requires:
\begin{enumerate}
  \item Certified $S_1 = \sum_{n=1}^{670} a(n)/n \cdot W_{\mathrm{afe}}(n/12)$ via Arb
        together with a rigorous truncation bound.  Numerically,
        $W_{\mathrm{afe}}(670/12) \approx 1.37\times10^{-3}$; the observed tail scale
        is about $8\times10^{-7}$, not $10^{-10}$.
  \item Certified $J$ via GL$_3$ Voronoi summation formula or explicit zero-free region
        for $L(s, \mathrm{sym}^2\Delta)$ [OBL M-3].
\end{enumerate}

\begin{thebibliography}{9}
\bibitem{GHL94} D.~Goldfeld, J.~Hoffstein, and D.~Lieman, Appendix: An effective
  zero-free region, Ann.\ Math.\ \textbf{140} (1994), 177--181.
\bibitem{GJ78} S.~Gelbart and H.~Jacquet, A relation between automorphic
  representations of $GL(2)$ and $GL(3)$, Ann.\ Sci.\ ENS \textbf{11} (1978), 471--542.
\bibitem{Sh75} G.~Shimura, On the holomorphy of certain Dirichlet series,
  Proc.\ London Math.\ Soc.\ \textbf{31} (1975), 79--98.
\bibitem{Vin58} I.~M.~Vinogradov, A new estimate of the function $\zeta(1+it)$,
  Izv.\ Akad.\ Nauk SSSR Ser.\ Mat.\ \textbf{22} (1958), 161--164.
\bibitem{Kor58} N.~M.~Korobov, Estimates of trigonometric sums and their applications,
  Uspehi Mat.\ Nauk \textbf{13} (1958), 185--192.
\end{thebibliography}

\end{document}
